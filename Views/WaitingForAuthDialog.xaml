<Window x:Class="FirmasApp.Views.WaitingForAuthDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Esperando autenticación"
        Height="430" Width="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="🔐" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <StackPanel>
                <TextBlock Text="Esperando autenticación de Keycloak" FontSize="18" FontWeight="Bold"/>
                <TextBlock x:Name="TxtMechanism" Text="..." Foreground="#666" FontSize="12" Margin="0,2,0,0"/>
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="1" x:Name="TxtStatus"
                   Text="Complete el login en el navegador."
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" x:Name="TxtUrlLabel"
                   Text="URL de autenticación (si el navegador no se abrió automáticamente):"
                   FontWeight="SemiBold" Margin="0,8,0,4"/>

        <Border Grid.Row="3" BorderBrush="#CCC" BorderThickness="1" CornerRadius="3" Padding="6" Background="#F8F8F8">
            <Grid>
                <TextBox x:Name="TxtAuthUrl" TextWrapping="Wrap" IsReadOnly="True" Background="Transparent"
                         BorderThickness="0" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="11"/>
            </Grid>
        </Border>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="📋 Copiar URL" Width="120" Height="32" Click="BtnCopiarUrl_Click" Margin="0,0,8,0"/>
            <Button Grid.Column="1" Content="🌐 Reabrir navegador" Width="160" Height="32" Click="BtnReabrirNavegador_Click" HorizontalAlignment="Left"/>
            <Button Grid.Column="2" Content="🧪 Probar protocolo" Width="140" Height="32" Click="BtnProbarProtocolo_Click" Margin="0,0,8,0"
                    ToolTip="Abre firmasapp://test en el navegador para verificar si el protocolo custom está funcionando"/>
        </Grid>

        <Grid Grid.Row="5" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="📥 Pegar URL manualmente" Height="36" Click="BtnPegarUrl_Click"
                    Background="#2196F3" Foreground="White" BorderThickness="0" FontWeight="SemiBold"
                    HorizontalAlignment="Stretch" Margin="0,0,8,0"
                    ToolTip="Pega la URL del callback o el code manualmente si el browser no devuelve a la app"/>
            <Button Grid.Column="1" Content="Cancelar" Width="100" Height="36" Click="BtnCancelar_Click" Margin="0,0,8,0"/>
            <Button Grid.Column="2" Content="Diagnóstico" Width="110" Height="36" Click="BtnDiagnostico_Click"/>
        </Grid>
    </Grid>
</Window>
