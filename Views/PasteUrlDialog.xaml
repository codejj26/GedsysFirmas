<Window x:Class="FirmasApp.Views.PasteUrlDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pegar URL de callback"
        Height="240" Width="540"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,8">
            Pegue la URL completa del callback o solo el código de autorización.
        </TextBlock>
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="#666" FontSize="11" Margin="0,0,0,8">
            Aparece en la barra de direcciones del navegador tras autenticarse.
        </TextBlock>
        <TextBox Grid.Row="2" x:Name="TxtPaste" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontFamily="Consolas"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancelar" Width="100" Height="30" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Aceptar" Width="100" Height="30" IsDefault="True" Click="BtnAceptar_Click"/>
        </StackPanel>
    </Grid>
</Window>
